{
  "name": "Uploadtest",
  "version": "0.0.1",
  "description": "A page to test the behaviour of file upload from different devices",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "echo \"Start the program via node index.js and specify the port as parameter.\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Felix De Montis <felix@demont.is>",
  "license": "MIT",
  "dependencies": {
    "jade": "~1.0.0",
    "formidable": "~1.0.14",
    "felix-couchdb": "~1.0.7"
  }
}
